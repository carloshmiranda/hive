name: Hive CEO
run-name: "CEO: ${{ github.event.action || github.event_name }} — ${{ github.event.client_payload.company || 'portfolio' }}"

on:
  repository_dispatch:
    types:
      - stripe_payment
      - cycle_complete
      - gate_approved
      - ceo_review
      - cycle_start

  workflow_dispatch:
    inputs:
      task:
        description: 'Override task (e.g. "review company X", "portfolio analysis")'
        required: false
        default: ''

permissions:
  contents: write
  issues: write
  pull-requests: write
  id-token: write

env:
  DATABASE_URL: ${{ secrets.DATABASE_URL }}

jobs:
  ceo:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Get tokens via OIDC
        id: auth
        run: |
          OIDC_TOKEN=$(curl -s -H "Authorization: bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" \
            "${ACTIONS_ID_TOKEN_REQUEST_URL}&audience=https://hive-phi.vercel.app" \
            | jq -r '.value')

          fetch_token() {
            local TYPE="$1" OUTPUT_KEY="$2" CRITICAL="${3:-false}"
            RESPONSE=$(curl -s -X POST "https://hive-phi.vercel.app/api/agents/token" \
              -H "Authorization: Bearer $OIDC_TOKEN" \
              -H "Content-Type: application/json" \
              -d "{\"token_type\":\"$TYPE\"}")
            VALUE=$(echo "$RESPONSE" | jq -r '.data.token // empty')
            if [ -n "$VALUE" ]; then
              echo "::add-mask::$VALUE"
              echo "${OUTPUT_KEY}=$VALUE" >> $GITHUB_OUTPUT
            else
              if [ "$CRITICAL" = "true" ]; then
                echo "FATAL: Failed to get $TYPE token: $(echo "$RESPONSE" | jq -r '.error // "unknown"')"
                exit 1
              else
                echo "WARNING: Failed to get $TYPE token (non-critical, continuing)"
              fi
            fi
          }

          fetch_token "claude" "claude_token" "true"
          fetch_token "github_pat" "gh_pat" "true"
          fetch_token "cron_secret" "cron_secret"

      - name: Build CEO context
        id: context
        env:
          DISPATCH_PAYLOAD: ${{ toJson(github.event.client_payload) }}
        run: |
          EVENT="${{ github.event_name }}"
          TASK="${{ github.event.inputs.task }}"

          # Helper: write multiline-safe output using heredoc delimiters
          set_output() {
            local key="$1" value="$2"
            echo "${key}<<GHEOF" >> $GITHUB_OUTPUT
            echo "${value}" >> $GITHUB_OUTPUT
            echo "GHEOF" >> $GITHUB_OUTPUT
          }

          if [ "$EVENT" = "repository_dispatch" ]; then
            ACTION="${{ github.event.action }}"
            PAYLOAD="${DISPATCH_PAYLOAD}"
            set_output "trigger" "repository_dispatch:${ACTION}"
            set_output "payload" "${PAYLOAD}"
          else
            set_output "trigger" "manual${TASK:+: $TASK}"
            set_output "payload" "{\"task\":\"${TASK}\"}"
          fi

      - name: Run CEO agent
        id: agent
        env:
          GH_PAT: ${{ steps.auth.outputs.gh_pat }}
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ steps.auth.outputs.claude_token }}
          claude_args: "--model claude-opus-4-20250514 --max-turns 40 --allowedTools Bash,Read,Write,Edit"
          prompt: |
            You are the CEO agent of Hive, an autonomous venture orchestrator.
            GH_PAT is available as $GH_PAT env var — use it for all gh commands: `GH_TOKEN="$GH_PAT" gh ...`

            TRIGGER: ${{ steps.context.outputs.trigger }}
            PAYLOAD: ${{ steps.context.outputs.payload }}

            Read BRIEFING.md, CLAUDE.md, and prompts/ceo.md for full context. Then:

            1. Query Neon (DATABASE_URL is set) for: active companies, pending approvals, recent metrics, open directives
            2. Determine validation phase for the company:
               - Fetch context from the Hive API: curl -s "$HIVE_URL/api/agents/context?agent=build&company_slug=<slug>" -H "Authorization: Bearer $CRON_SECRET"
               - The response includes business_type, validation (score, phase, gating_rules, forbidden, kill_signal)
               - Follow the validation phase rules in prompts/ceo.md — the phase determines what you can plan
               - Load the original Scout proposal from the approvals table (gate_type='new_company', status='approved')
               - Load ALL research reports for this company (market_research, competitive_analysis, seo_keywords)
               - Load the previous product spec: SELECT content FROM research_reports WHERE company_id = '<id>' AND report_type = 'product_spec' — UPDATE it, don't rewrite from scratch
               - After planning, ALWAYS save the updated product_spec to research_reports (UPSERT on company_id + report_type = 'product_spec')
            3. Based on the trigger:
               - stripe_payment: Review revenue impact, update company status if first revenue, plan next actions
               - cycle_complete: Score the cycle 1-10 using phase-appropriate criteria from prompts/ceo.md
               - gate_approved: Route the approval. If gate_type is 'new_company', output needs_provisioning: true so Engineer can scaffold the repo, Vercel project, Neon DB, and Stripe product. Include the company slug.
               - cycle_start: Start a new cycle for the specified company. Load research reports, determine lifecycle mode, create a cycle plan with engineering_tasks and growth_tasks.
               - ceo_review: Review and merge PRs from the Engineer agent. Payload has `company` and `pr_number`.
                 IMPORTANT: After reviewing the PR from the payload, ALWAYS check for additional open PRs and review those too:
                 GH_TOKEN="$GH_PAT" gh pr list --repo carloshmiranda/<company> --state open --json number,title

                 STEP 1 — Gather context (repeat for each open PR):
                 ```bash
                 GH_TOKEN="$GH_PAT" gh pr view <pr_number> --repo carloshmiranda/<company> --json title,body,additions,deletions,changedFiles,mergeable
                 GH_TOKEN="$GH_PAT" gh pr diff <pr_number> --repo carloshmiranda/<company>
                 GH_TOKEN="$GH_PAT" gh pr checks <pr_number> --repo carloshmiranda/<company>
                 ```

                 STEP 2 — Hard gates (reject immediately if any fail):
                 - CI/build checks pass (from gh pr checks)
                 - No secrets in diff (API_KEY, SECRET, PASSWORD, Bearer, hardcoded tokens, .env values)
                 - No destructive DB migrations without rollback plan
                 - Diff size reasonable (>1000 lines or >20 files = flag as high-risk)

                 STEP 3 — Task alignment:
                 - PR maps to a planned engineering_task from the current cycle plan
                 - Acceptance criteria from the task are met
                 - No scope creep (changes unrelated to the assigned task)

                 STEP 4 — Code quality scan (focus on new files + modified API routes):
                 - API routes have error handling (try/catch) and return { ok, data?, error? }
                 - SQL uses parameterized queries (no string interpolation)
                 - No console.log/debug artifacts left behind
                 - No hardcoded values (URLs, prices, emails should come from config/DB)
                 - New API routes call requireAuth() or have documented reason not to

                 STEP 4b — Design quality scan (for PRs that touch .tsx/.css files):
                 - No gradients (bg-gradient-to-*, from-*, via-*) — solid colors only
                 - No raw hex colors in components — must use design tokens from globals.css (text-brand, bg-accent, etc.)
                 - No duplicate sections on the same page (two feature grids, two CTA blocks, two hero sections)
                 - Max 2 font weights used (font-normal, font-bold — no font-black, font-extrabold)
                 - No placeholder content (lorem ipsum, "Coming soon", stock photo URLs)
                 - Landing page changes: verify single CTA per viewport, proper section spacing
                 If ANY design violation found: add +2 to risk score and list violations in review summary.

                 STEP 5 — Risk score:
                 | Factor | Points |
                 | Touches auth/payments/user data | +3 |
                 | Changes DB schema | +3 |
                 | Adds new dependencies | +2 |
                 | >500 lines changed | +2 |
                 | >10 files changed | +2 |
                 | New API routes | +1 |
                 | Touches landing page (page.tsx) | +1 |
                 | Design violations found in 4b | +2 |
                 | Only content/copy changes (no code) | -2 |

                 Score 0-3: Auto-merge. Score 4-6: Merge + log detailed summary. Score 7+: Do NOT merge, create approval gate for Carlos.

                 STEP 6 — Decision:
                 - MERGE: `GH_TOKEN="$GH_PAT" gh pr merge <pr_number> --repo carloshmiranda/<company> --merge`
                 - REJECT: `GH_TOKEN="$GH_PAT" gh pr review <pr_number> --repo carloshmiranda/<company> --request-changes --body "<issues>"`
                 - After merging, check for more open PRs: `GH_TOKEN="$GH_PAT" gh pr list --repo carloshmiranda/<company> --state open --json number,title`

                 STEP 7 — Output structured review:
                 ```json
                 { "pr_review": { "pr_number": N, "company": "<slug>", "task_id": "<id>",
                   "hard_gates_passed": true, "task_aligned": true, "risk_score": 3,
                   "decision": "merge|reject|escalate", "summary": "<one line>" },
                   "pr_merged": true/false }
                 ```
               - issue/directive: Decompose into tasks, assign to agents
               - PR opened: Review for strategic alignment
               - manual: Execute the specified task
            4. Write your plan to the cycles table using the output format matching your lifecycle mode
            5. Output JSON with: { "actions": [...], "needs_research": bool, "needs_feature": bool, "needs_provisioning": bool, "company": "slug" }

            IMPORTANT: Write ALL decisions to the database. Update BRIEFING.md if state changed.

      - name: Log failure to DB
        if: always() && steps.agent.outcome == 'failure'
        run: |
          npm install --no-save postgres 2>/dev/null
          # Try action output first, then known fallback path
          EXEC_FILE="${{ steps.agent.outputs.execution_file }}"
          if [ ! -f "$EXEC_FILE" ] || [ -z "$EXEC_FILE" ]; then
            EXEC_FILE="/home/runner/work/_temp/claude-execution-output.json"
          fi
          SUBTYPE=$(jq -r '.[-1].subtype // "unknown"' "$EXEC_FILE" 2>/dev/null || echo "unknown")
          TURNS=$(jq -r '.[-1].num_turns // 0' "$EXEC_FILE" 2>/dev/null || echo "0")
          COST=$(jq -r '.[-1].total_cost_usd // 0' "$EXEC_FILE" 2>/dev/null || echo "0")

          node -e "
          const postgres = require('postgres');
          const sql = postgres(process.env.DATABASE_URL);
          async function log() {
            await sql\`
              INSERT INTO agent_actions (agent, action_type, status, error, output, started_at, finished_at)
              VALUES ('ceo', '${{ steps.context.outputs.trigger }}', 'failed',
                \${'error_' + '${SUBTYPE}' + ': exhausted after ${TURNS} turns'},
                \${JSON.stringify({subtype: '${SUBTYPE}', turns: parseInt('${TURNS}'), cost: parseFloat('${COST}')})}::jsonb,
                NOW() - INTERVAL '${TURNS} minutes', NOW())
            \`;
            await sql.end();
          }
          log().catch(e => console.error('DB log failed:', e.message));
          "
          echo "Logged CEO failure: $SUBTYPE ($TURNS turns)"

      - name: Chain dispatch
        if: always() && steps.agent.outcome == 'success'
        env:
          GH_TOKEN: ${{ steps.auth.outputs.gh_pat }}
          CRON_SECRET: ${{ steps.auth.outputs.cron_secret }}
          HIVE_URL: https://hive-phi.vercel.app
          DISPATCH_PAYLOAD: ${{ toJson(github.event.client_payload) }}
        run: |
          set +e  # Prevent grep/jq exit codes from crashing the step
          RESULT=$(jq -r '[.[] | select(.type == "assistant")] | last | [.message.content[] | select(.type == "text") | .text] | join("\n")' "${{ steps.agent.outputs.execution_file }}" 2>/dev/null || echo "{}")
          REPO="${{ github.repository }}"
          TRIGGER="${{ steps.context.outputs.trigger }}"
          TRACE_ID=$(echo "${DISPATCH_PAYLOAD}" | jq -r '.trace_id // empty' 2>/dev/null || echo "")
          echo "Chain dispatch parsing last assistant output (${#RESULT} chars)"
          echo "Trigger: $TRIGGER"
          echo "Trace ID: $TRACE_ID"

          dispatch() {
            curl -s -X POST "https://api.github.com/repos/$REPO/dispatches" \
              -H "Authorization: token $GH_TOKEN" \
              -H "Accept: application/vnd.github.v3+json" \
              -d "{\"event_type\":\"$1\",\"client_payload\":$2}"
            echo "  Dispatched: $1"
          }

          # Extract company: try output first, then input payload (multiple fallbacks)
          COMPANY=$(echo "$RESULT" | grep -oP '"company"\s*:\s*"\K[^"]+' | head -1 || echo "")
          if [ -z "$COMPANY" ]; then
            COMPANY=$(echo "${DISPATCH_PAYLOAD}" | jq -r '.company // ""' 2>/dev/null || echo "")
          fi
          # Also extract company_id for downstream agents
          COMPANY_ID=$(echo "$RESULT" | grep -oP '"company_id"\s*:\s*"\K[^"]+' | head -1 || echo "")
          if [ -z "$COMPANY_ID" ]; then
            COMPANY_ID=$(echo "${DISPATCH_PAYLOAD}" | jq -r '.company_id // ""' 2>/dev/null || echo "")
          fi
          GITHUB_REPO=""
          echo "Company: $COMPANY | Company ID: $COMPANY_ID"

          # Guard: if no company found, skip all dispatches
          if [ -z "$COMPANY" ]; then
            echo "WARNING: No company slug found in output or payload — skipping dispatches"
            echo "Chain dispatch complete (no company)"
            exit 0
          fi

          # Detect signals: strict JSON-only matching (no prose matching to avoid false positives)
          check_signal() {
            echo "$RESULT" | grep -qP "\"${1}\"\s*:\s*true" && return 0
            return 1
          }

          # Build reusable payload with company + company_id + trace_id
          BASE_PAYLOAD="\"company\":\"$COMPANY\",\"company_id\":\"$COMPANY_ID\""
          if [ -n "$TRACE_ID" ]; then
            BASE_PAYLOAD="$BASE_PAYLOAD,\"trace_id\":\"$TRACE_ID\""
          fi

          # Only dispatch downstream agents for cycle_start (not cycle_complete — avoids infinite loop)
          IS_CYCLE_START=false
          echo "$TRIGGER" | grep -q "cycle_start" && IS_CYCLE_START=true

          if [ "$IS_CYCLE_START" = "true" ]; then
            # Chain to Engineer for provisioning (only if explicitly flagged)
            if check_signal "needs_provisioning"; then
              dispatch "new_company" "{\"source\":\"ceo\",$BASE_PAYLOAD}"
            fi

            # Chain to Scout if research needed
            if check_signal "needs_research"; then
              dispatch "research_request" "{\"source\":\"ceo\",$BASE_PAYLOAD}"
            fi

            # Define dispatch helpers (must be before first call)
            dispatch_worker() {
              local WORKER_PAYLOAD="{\"company_slug\":\"$COMPANY\",\"agent\":\"$1\",\"trigger\":\"ceo_plan\""
              if [ -n "$TRACE_ID" ]; then
                WORKER_PAYLOAD="$WORKER_PAYLOAD,\"trace_id\":\"$TRACE_ID\""
              fi
              WORKER_PAYLOAD="$WORKER_PAYLOAD}"
              curl -s -X POST "$HIVE_URL/api/agents/dispatch" \
                -H "Authorization: Bearer $CRON_SECRET" \
                -H "Content-Type: application/json" \
                -d "$WORKER_PAYLOAD" \
                -o /dev/null -w "  Worker $1: HTTP %{http_code}\n" || true
            }
            dispatch_to_company_repo() {
              local WORKFLOW="$1" SUMMARY="$2"
              # Get company's github_repo from DB (cached after first call)
              if [ -z "$GITHUB_REPO" ]; then
                GITHUB_REPO=$(node -e "
                  const postgres = require('postgres');
                  const sql = postgres(process.env.DATABASE_URL);
                  sql\`SELECT github_repo FROM companies WHERE slug = '${COMPANY}' LIMIT 1\`
                    .then(r => { console.log(r[0]?.github_repo || ''); sql.end(); })
                    .catch(() => { console.log(''); process.exit(0); });
                " 2>/dev/null)
              fi
              if [ -n "$GITHUB_REPO" ]; then
                curl -s -X POST "https://api.github.com/repos/${GITHUB_REPO}/actions/workflows/${WORKFLOW}/dispatches" \
                  -H "Authorization: token $GH_TOKEN" \
                  -H "Accept: application/vnd.github.v3+json" \
                  -H "Content-Type: application/json" \
                  -d "{\"ref\":\"main\",\"inputs\":{\"company_slug\":\"$COMPANY\",\"trigger\":\"ceo_plan\",\"task_summary\":\"$SUMMARY\"}}" \
                  -o /dev/null -w "  Company $WORKFLOW: HTTP %{http_code}\n" || dispatch_worker "$3"
              else
                dispatch_worker "$3"
              fi
            }

            # Chain to Engineer → dispatch directly to company repo (free Actions)
            # Falls back to Hive Engineer only if no company repo exists
            dispatch_to_company_repo "hive-build.yml" "Feature build for $COMPANY" ""
            if [ -z "$GITHUB_REPO" ]; then
              dispatch "feature_request" "{\"source\":\"ceo\",$BASE_PAYLOAD}"
            fi

            # Growth → company repo (Claude, free) with Vercel fallback
            echo "$RESULT" | grep -qiP "growth_tasks|growth_strategy|content.?calendar" && \
              dispatch_to_company_repo "hive-growth.yml" "Growth cycle for $COMPANY" "growth"
            # Outreach → Vercel serverless (Gemini, doesn't need repo access)
            echo "$RESULT" | grep -qiP "outreach_tasks|lead.?list|cold.?email" && \
              dispatch_worker "outreach"

            # Dispatch cycle_complete ONCE (for review/reflection after agents finish)
            dispatch "cycle_complete" "{\"source\":\"ceo\",$BASE_PAYLOAD}"
          fi

          # For gate_approved: only dispatch the specific action needed
          if echo "$TRIGGER" | grep -q "gate_approved"; then
            if check_signal "needs_provisioning"; then
              dispatch "new_company" "{\"source\":\"ceo\",$BASE_PAYLOAD}"
            fi
          fi

          # cycle_complete / ceo_review: trigger post-cycle consolidation + chain next company
          # This feeds CEO review outcomes back into the playbook (learning loop)
          if echo "$TRIGGER" | grep -qE "cycle_complete|ceo_review"; then
            echo "Triggering post-cycle consolidation..."
            curl -s -X POST "$HIVE_URL/api/agents/consolidate" \
              -H "Authorization: Bearer $CRON_SECRET" \
              -H "Content-Type: application/json" \
              -d "{\"company_slug\":\"$COMPANY\"}" \
              -o /dev/null -w "  Consolidation: HTTP %{http_code}\n" || true

            # Chain dispatch: cycle done → health gate → next company
            echo "Chaining to next company cycle..."
            CHAIN_RESULT=$(curl -s -X POST "$HIVE_URL/api/dispatch/cycle-complete" \
              -H "Authorization: Bearer $CRON_SECRET" \
              -H "Content-Type: application/json" \
              -d "{\"agent\":\"ceo\",\"company\":\"$COMPANY\",\"status\":\"cycle_complete\",\"action_type\":\"cycle_review\"}")
            echo "  Chain result: $CHAIN_RESULT"
          fi

          echo "Chain dispatch complete"
