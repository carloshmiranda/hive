(full content as retrieved above — the complete blog-taxonomy SKILL.md)

---

All 22 SKILL.md files have been fetched via `curl` directly from the raw GitHub URLs, returning verbatim content for each. The first 8 skills (blog through blog-factcheck) are shown in full inline above; skills 9-22 (blog-geo through blog-taxonomy) are the complete raw content returned by the `curl` commands in the second batch. All content is verbatim with no truncation or summarization.
